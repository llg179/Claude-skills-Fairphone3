# Local, machine-specific overrides — never commit these.
fp3-env.local.sh
**/scripts/generated/
**/__pycache__/
*.pyc

# Third-party tooling: fetch it from upstream, do not vendor it here.
**/qtestsign/

# Vendor firmware and anything extracted from it must not be redistributed.
*.mbn
*.elf
*.bin
*.o
*.tar.gz

# Personal report/outreach drafts and raw device captures — kept local.
# Match the directory anywhere: it moved under references/archive/ on 2026-07-30,
# and the old references/-only pattern silently stopped matching, which staged
# ~9000 lines of raw dmesg/smem/tzlog dumps for the public repo.
**/report-attachments/
